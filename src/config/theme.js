// ============================================================
// THEME — Colors, fonts, and brand tokens
// ============================================================

/** Brand accent color — used for active nav tabs, filter pills */
export const BRAND = { cyan: "#0050AE" };

/** Font stack — change this to use a different typeface */
export const FONT = "Inter, -apple-system, BlinkMacSystemFont, sans-serif";

/**
 * Color palette (light mode only)
 * bg     — page background
 * text   — primary text color (ether.fi navy)
 * sub    — secondary text
 * muted  — tertiary/disabled text
 * dim    — very subtle text
 * border — card/section borders
 * card   — card background
 * cardH  — card hover background
 * over   — nav overlay background (semi-transparent)
 * modal  — modal background
 */
export const c = {
  bg: "#ffffff",
  text: "#19163B",
  sub: "#333342",
  muted: "#555568",
  dim: "#888898",
  border: "rgba(0,0,0,0.08)",
  card: "#ffffff",
  cardH: "#f5f5f8",
  over: "rgba(255,255,255,0.92)",
  modal: "#ffffff",
};
